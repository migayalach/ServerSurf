const createLevel = (name) => {
  return name;
};

const getNameLevel = (name) => {
  return name;
};

const getLevelId = (id) => {
  return id;
};

const getAllLevel = () => {
  return 'TODOS LOS NIVELES';
};

module.exports = { 
  createLevel,
  getNameLevel,
  getLevelId,
  getAllLevel
};
